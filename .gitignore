# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs (created at runtime)
logs/
*.log

# Test reports
test_report.json
.coverage
htmlcov/
.pytest_cache/

# Build artifacts
build/
dist/
*.exe
!app/firewall/*.exe

# Installer output
dist/*.exe
Output/

# OS
.DS_Store
Thumbs.db

# Application specific
*.lock
cleanup_*.py

# User data — NEVER commit (contains real IPs, MACs, device fingerprints)
app/data/
!app/data/.gitkeep
app/data/*.json
app/data/*.jsonl
app/data/**/*.jsonl
app/data/**/*.jsonl.bak
app/data/*.hmac
*.hmac
app/data/*.db
app/data/*.db-journal
app/data/*.backup.*
app/data/**/*.backup.*
app/data/profiles/
dist/app/data/

# Environment files
.env
.env.*
!.env.example
.envrc
.direnv/

# Credentials and key material — NEVER commit
*.pem
*.key
*.pfx
*.p12
*.jks
*.keystore
id_rsa
id_ed25519
id_ecdsa
id_dsa
*.priv
*_rsa
*_ed25519
.secrets
.secrets.yaml
credentials.json
credentials.yaml

# Pre-commit / secret-scanner local state
.secrets.baseline.local

# Runtime caches
device_cache.json
session_history.json
**/patch_monitor_state.json

# Encrypted secrets store — NEVER commit
secrets.enc.json
app/data/secrets.enc.json

# Runtime audit trail (contains masked but still sensitive event data)
app/data/audit.jsonl

# Crash dumps & stray runtime traces
FATAL_CRASH.txt
crash-trace.txt
launch_error.txt
/query

# Stale audit/debug artifacts
AUDIT_*.md

# Clumsy runtime files (generated at runtime)
app/firewall/presets.ini

# WinDivert external sample/test executables (not needed for DupeZ)
app/firewall/clumsy_src/external/**/*.exe

# Settings temp file (at
# Auto-generated by build_common.py per variant build
app/firewall_helper/_build_default.py
.vercel
.env*.local
